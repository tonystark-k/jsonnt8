name: Debug TradingEconomics

on:
  workflow_dispatch:   # manual trigger only — never runs automatically

jobs:
  debug:
    runs-on: ubuntu-latest
    timeout-minutes: 3

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '22'

      - name: Inspect tradingeconomics.com HTML
        run: node scripts/debug-tradingeconomics.js
